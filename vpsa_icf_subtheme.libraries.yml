# SMACSS CSS categories: base, layout, component, state, theme.
# Uncomment additional entries as CSS is split into separate files.

allpages:
  css:
    theme:
      dist/css/main.css: { }
#      dist/css/print.css: { media: print }
#    base:
#      dist/css/base.css: { weight: 100 }
#    layout:
#      dist/css/layout.css: {}
#    component:
#      dist/css/components.css: {}
#    state:
#      dist/css/state.css: {}

fellows_tags_filter:
  js:
    # Run in <head> so the custom select mounts before first paint when possible.
    js/fellows-tags-filter.js: { header: true, weight: -20 }
  dependencies:
    - core/drupal
    - core/once
