# SMACSS CSS categories: base, layout, component, state, theme.
# Uncomment additional entries as CSS is split into separate files.

allpages:
  css:
    theme:
      dist/css/main.css: { }
#      dist/css/print.css: { media: print }
#    base:
#      dist/css/base.css: { weight: 100 }
#    layout:
#      dist/css/layout.css: {}
#    component:
#      dist/css/components.css: {}
#    state:
#      dist/css/state.css: {}
  js:
    # Keep banner edit/preview canvases in sync with Banner Style / Overlay.
    js/banner-variant-preview.js: {}
  dependencies:
    - core/drupal
    - core/once

fellows_tags_filter:
  js:
    # Footer (default) so Drupal/once are always available before this runs.
    js/fellows-tags-filter.js: {}
  dependencies:
    - core/drupal
    - core/once
